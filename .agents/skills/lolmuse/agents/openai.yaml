interface:
  display_name: "Lolmuse"
  short_description: "Repo-specific patterns and workflows for lolmuse"
  default_prompt: "Use the lolmuse repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true